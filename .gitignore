# Dependencias (el proyecto no tiene de runtime, pero por si acaso)
node_modules/
npm-debug.log*

# Entorno y secretos
.env
.env.local
*.key
*.pem

# Sistema operativo
.DS_Store
Thumbs.db

# Temporales de la raíz (las bitácoras de projects/ SÍ se versionan)
*.tmp
/jarvis-core.log

# DSH_HOME local de pruebas (contiene credenciales: NUNCA versionar)
.dsh-home/

# NOTA: chat-config.json NO se ignora a proposito: la preferencia de modelo es
# parte de "como tengo el sistema" y debe sobrevivir a la muerte de la SD.
